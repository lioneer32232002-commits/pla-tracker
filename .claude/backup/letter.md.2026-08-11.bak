# 給未來 session 的信

> 寫於 2026-07-04，Fable 5 制度建立 session。這封信放三件沒人問、但你遲早會撞上的
> 事，以及這套制度最可能怎麼爛掉。接大型工作前讀一遍。

## 一、真正的產品是「無人值守的管線」，你的頭號職責是別弄壞它

這個專案的核心價值不是網站本身，是**使用者不用管它也天天正確更新**。三個定時炸彈：

1. **寫死的 API 型號**：`scripts/fetch_and_update.py:288,310` 用 `claude-opus-4-6`，
   `scripts/send_daily_email.py:356` 用 `claude-sonnet-4-6`。Anthropic 退役這些型號的
   那天，管線就死。症狀會是 20:00 班寄來的失敗信（model not found 類錯誤）。
   修法：換成當時的現役型號即可，擷取 prompt（`_EXTRACT_SPEC`）不用動。若你知道
   退役公告已出，主動提醒使用者提前換。
2. **`daily_update.yml` 的 git add 白名單**：新頁面沒加進去＝線上停更但 CI 綠燈。
   細節在 [project-reference.md](project-reference.md)。
3. **ANTHROPIC_API_KEY / GMAIL secrets 過期或額度用盡**：你在本機查不到 secrets，
   只能從 CI log 的錯誤訊息判斷。這類問題**你修不了**，正確動作是明確告訴使用者
   要去 GitHub repo Settings → Secrets 換哪一把。

改任何 CI 相關檔案前，想一句話：「如果這個改動是錯的，明天中午的自動更新
會發生什麼？」答不出來就先用 `workflow_dispatch` 手動觸發驗證。

## 二、這個 repo 住在 OneDrive 的中文路徑裡，這不是你的錯，但會咬你

`C:\Users\User\OneDrive\02_創作\14_AI TEST\pla-tracker` —— 同步鎖檔、
`index.lock` 殘留、cp950 主控台、路徑含空格與中文。已知緩解都在
[00-diagnosis.md](00-diagnosis.md) 風險三。額外兩點：

- 遇到「檔案明明寫了卻讀到舊內容」「git 說檔案變了但 diff 是空的」→ 先懷疑
  OneDrive，等幾秒重試，不要開始亂改程式。
- 若哪天環境問題頻繁到影響工作，值得**建議**使用者把 repo 搬出 OneDrive
  （git 本身就是備份，GitHub 上有完整歷史）。這是建議不是行動——搬家要使用者
  自己決定。

## 三、使用者的真正目標在站外：把數據變成有人看的內容

網站是手段，目的是 Threads 與部落格的影響力（對比對手 platracker.com 的差異化：
繁中在地、中線逾越比例視覺化、法理框架）。這意味著：

- 對「讓數據更容易被引用/分享」的功能（嵌入、PNG 下載、社群卡）要積極；
  對「工程上有趣但讀者無感」的重構要克制。
- 功能 F（社群卡圖）已於 2026-07-31 上線＝`card.html`：直式 1080×1350、客戶端
  canvas 自繪、一鍵下載 PNG。細節與禁忌見 project-reference「分享圖卡 card.html」。
  使用者自己跑外宣，你的工作是讓素材一鍵可得。
- 文案語感、對外說法是品味題——永遠給選項，不要替使用者定調
  （[judgment.md](judgment.md) 第 6 節）。

## 這套制度最可能的退化方式（與預防）

1. **沒人讀規則檔**：路由表形同虛設，session 靠預設習慣工作，坑重踩。
   預防：CLAUDE.md 已把路由表做成「做 X 前先讀 Y」的觸發式（不是「有空看看」）。
   如果你現在才第一次看到這封信而你已在此專案工作過多次——這就是退化的證據，
   請在回報裡告訴使用者。
2. **教訓只進不出**：規則檔膨脹成沒人讀的長清單，重要規則被淹沒。
   預防：[maintenance.md](maintenance.md) 第 4 節的行數上限與合併規則。上限是硬的。
3. **事實過期導致全面失信**：某條規則的路徑/行數/型號錯了，弱模型發現後開始
   懷疑所有規則。預防：規則寫「查證方法」優於寫「事實本身」（所以本信引用行號
   都附檔名，過期可重查）；發現過期事實立即修（維護協議允許自行改）。
4. **驗證儀式化**：跑了 validate 但沒看輸出、派了驗收 agent 但只給 PASS 也照單全收。
   預防：驗收模板規定「不確定算 FAIL」；完成定義要求附執行證據，不是聲明。

## 交接欄（session 中斷時把未完成事項寫在這裡）

- 2026-07-04 制度建立 session：交付 A–G 全部完成。無未完成項。
- 2026-07-23 大改版 session：已上線項目見 project-reference「已知歷史事件」同日條目。
  當時列的三項待辦，2026-08-05 清查結果：
  1. ~~建「美製武器實戰檔案」區~~ **已完成**：`arsenal/{index,harpoon,patriot,himars}.html`
     與 `en/arsenal/` 皆已上線，導覽列「軍購」為入口。原始草稿放在 gitignore 的
     `output/research_2026-07-23/`，該資料夾已不存在——內容已在網站上，不需保留。
  2. pla-tracker × skyfaring 串接：**調查報告 `landscape_survey.md` 已遺失**（同樣放在
     已消失的 output/）。現存資訊只剩一句：A/B/C 三案中 **B＝在 pla-tracker 加 CSV 驅動
     的「研究導讀」清單區塊**為當時推薦案，A/C 內容無存底。要推進的話：直接就 B 案
     決定，或重做一次調查——**動工前先問使用者**（屬結構性變更）。
  3. ~~日報信新版首次實跑~~ **已過**（2026-07-24 中午）。
